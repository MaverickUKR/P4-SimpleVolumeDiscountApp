query RunInput {
  cart {
    lines {
      id
      quantity
      merchandise {
        ... on ProductVariant {
          id
          product {
             metafield(namespace: "product_data", key: "volume_discount") {
              value
            }
          }
        }
      }
    }
  }
}
